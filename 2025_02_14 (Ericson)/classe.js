export const soma = (a, b) => {
  return a + b;
};

export const subtracao = (a, b) => {
  return a - b;
};

export const multiplicacao = function (a, b) {
  return a * b;
};

export const divisao = (a, b) => {
  return a / b;
};

export const parouimpar = (x) => {
  if (x % 2 == 0) {
    alert(`O número é par`);
  } else {
    alert(`O número é ímpar`);
  }
};
